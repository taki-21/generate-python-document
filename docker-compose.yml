version: '3.8'

services:
  python-wrapper:
    container_name: python_docment

    build:
      context: ./
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"
    tty: true
    volumes:
      - .:/code/
      - ${PIP_CACHE_DIR:-cache-sample-wrapper}:/root/.cache

volumes:
  cache-sample-wrapper:
